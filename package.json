{
  "name": "lean-deck",
  "private": true,
  "scripts": {
    "build": "marp deck.md -o deck.html",
    "build:pdf": "marp deck.md -o deck.pdf",
    "build:pptx": "marp deck.md -o deck.pptx",
    "watch": "marp -w deck.md -o deck.html",
    "preview": "marp -s deck.md"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.0.4"
  }
}
